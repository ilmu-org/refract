openapi: "3.0.3"
info:
  title: Cycle Test
  version: "1.0"
paths:
  /items:
    get:
      summary: Test
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "./a.yaml#/SchemaA"

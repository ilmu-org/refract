openapi: "3.0.3"
info:
  title: HTTP Ref Test
  version: "1.0"
paths:
  /items:
    get:
      summary: Test
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "https://example.com/schemas/pet.yaml#/Pet"

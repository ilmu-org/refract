openapi: "3.0.3"
info:
  title: Basic External Ref Test
  version: "1.0"
paths:
  /pets:
    get:
      summary: List pets
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "./schema.yaml#/Pet"

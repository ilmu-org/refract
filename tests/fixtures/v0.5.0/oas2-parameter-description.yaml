swagger: "2.0"
info:
  title: Test API
  version: "1.0"
host: api.example.com
paths:
  /pets/{petId}:
    get:
      operationId: getPet
      summary: Get a pet by ID
      parameters:
        - name: petId
          in: path
          required: true
          type: string
      responses:
        "200":
          description: OK

#!/bin/sh
# pre-push — mirrors CI checks exactly
# Requires: cargo-audit, cargo-deny, cargo-nextest

set -e

BOLD='\033[1m'
RED='\033[0;31m'
RESET='\033[0m'

fail() {
    printf "${BOLD}${RED}pre-push: FAILED — %s${RESET}\n" "$1" >&2
    exit 1
}

step() {
    printf "pre-push: running %s ...\n" "$1"
}

# ── 1. Formatting ──────────────────────────────────────────────────────────

step "cargo fmt --check"
cargo fmt --check || fail "cargo fmt --check (run 'cargo fmt' to fix)"

# ── 2. Linting ────────────────────────────────────────────────────────────

step "cargo clippy --all-targets --all-features"
cargo clippy --all-targets --all-features -- -D warnings -W clippy::pedantic \
    || fail "cargo clippy (fix the warnings above)"

# ── 3. Tests ──────────────────────────────────────────────────────────────

step "cargo nextest run --all-features"
cargo nextest run --all-features || fail "cargo nextest run --all-features"

# ── 4. Security audit ─────────────────────────────────────────────────────

step "cargo audit"
cargo audit || fail "cargo audit (vulnerability found)"

# ── 5. License and dependency policy ──────────────────────────────────────

step "cargo deny check"
cargo deny check || fail "cargo deny check (policy violation)"

# ── 6. Documentation build ────────────────────────────────────────────────

step "cargo doc --no-deps"
RUSTDOCFLAGS="-D warnings" cargo doc --no-deps || fail "cargo doc (fix rustdoc warnings above)"

printf "pre-push: all checks passed.\n"
